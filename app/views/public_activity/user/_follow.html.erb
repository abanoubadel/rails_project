<% if activity.trackable %>
<%= link_to activity.owner.name, activity.owner if activity.owner %> is now following <%= if current_user == activity.trackable then 'you' else link_to activity.trackable.name, activity.trackable end %>
<% end %>