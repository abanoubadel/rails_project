<% if activity.trackable %>
<%= link_to activity.owner.name, activity.owner if activity.owner %> has unfollowed <%= if current_user == activity.trackable then 'you' else link_to activity.trackable.name, activity.trackable end %>
<% end %>